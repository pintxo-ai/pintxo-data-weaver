/**
 * @description Represents the data structure for a single chain.
 */
export interface ChainData {
    gecko_id: string;
    tvl: number;
    tokenSymbol: string;
    cmcId: string;
    name: string;
    chainId: number;
}